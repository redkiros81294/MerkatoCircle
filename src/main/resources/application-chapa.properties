# Activated with: mvn spring-boot:run -Dspring-boot.run.profiles=chapa
# Requires a real CHAPA_SECRET_KEY environment variable (a test-mode key looks like
# CHASECK_TEST-...; get one free at dashboard.chapa.co). Never hardcode it here.
chapa.secret-key=${CHAPA_SECRET_KEY}
